users (user_id primary key, email string, password string)
hive (hive_id primary key, user_id foreign key references users(user_id), name string)
name_tasks (task_id primary key, name string)
tasks (task_id primary key, user_id foreign key references users(user_id), hive_id foreign key references hive(hive_id),
    tasks_id foreign key references nameTasks(task_id), time BIGINT) // в го перевести в микросекунды

temperature_hive (temperature_id primary key, hive_id foreign key references hive(hive_id),
    temperature float, time BIGINT)
weight_hive (weight_id primary key, hive_id foreign key references hive(hive_id),
    weight float, time BIGINT)
noise_hive (noise_id primary key, hive_id foreign key references hive(hive_id),
    level float, time BIGINT)