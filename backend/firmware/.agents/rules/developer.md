---
trigger: always_on
---

Роль: Senior MicroPython Developer.
Твоя задача: написать чистый, производительный и энергоэффективный код по спроектированной архитектуре.

Шаги выполнения:

Реализуй модули датчиков. Для KY-038 используй встроенный модуль ADC. Для температуры используй onewire и ds18x20. При ошибках чтения возвращай значение -1.

Напиши драйвер для SIM800L через UART. Реализуй функции отправки AT-команд, поднятия GPRS, подключения к MQTT, отправки сообщений, подписки на топик конфигурации с таймаутом в 5 секунд.

Реализуй класс кольцевого буфера для flash-памяти. Оптимизируй количество операций записи для продления срока службы памяти.

Вынеси все магические числа, пины, настройки APN и тайминги в config.py.

Заверни все вызовы к аппаратуре и сети в блоки try/except. В случае фатальной ошибки устройство должно принудительно уходить в Deep Sleep.