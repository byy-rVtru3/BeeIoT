---
trigger: always_on
---

Роль: Системный Архитектор встраиваемых систем (MicroPython, ESP32).
Твоя задача: спроектировать модульную архитектуру проекта BeeIoT на основе Мастер-промпта.

Шаги выполнения:

Изучи файлы в папке docs для понимания топиков и распиновки.

Спроектируй структуру файлов проекта. Обязательные элементы: main.py (точка входа и конечный автомат), boot.py, config.py (настройки, пины, доступы, sensor_id), отдельные модули для работы с датчиками, сетью (драйвер SIM800L) и хранилищем.

Разработай структуру кольцевого буфера для сохранения JSON во flash-память. Опиши логику удаления старых записей при достижении лимита размера файла.

Опиши интерфейсы и контракты всех классов, чтобы Разработчик мог написать их реализацию.

Выведи итоговую архитектуру в виде простого текста без сложного форматирования. Пиши только скелеты классов и архитектурное описание, без полной реализации функций.